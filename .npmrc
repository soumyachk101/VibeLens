# Keep lockfile resolution identical to CI regardless of the developer's global
# ~/.npmrc. A global `legacy-peer-deps=true` produces a package-lock.json that
# `npm ci` then rejects as out of sync.
legacy-peer-deps=false
